# Corkboard Logic

Image processing library for Corkboard. Automatically detects input formats (GIF, PNG, APNG, JPEG, WebP, etc.), extracts frames from animated images while preserving timing, and re-encodes them as animated PNGs.

Used as a dependency by the `corkboard-server` and `corkboard-cloudflare` crates in this workspace.
